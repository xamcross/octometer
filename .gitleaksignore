# Each line ignores one gitleaks finding by its fingerprint. gitleaks
# scans each commit of the history, so a later commit that renames or
# removes a value does not clear an old finding on its own; the exact
# fingerprint stays needed here.
#
# Each fingerprint below names a commit of a pull request branch, not
# of `main`. The pull request's own secret scan reads the whole branch
# history, so the entry stays needed until the merge. A squash merge
# writes one new commit on `main`, with a fingerprint of its own, so
# each entry below dies at that merge. Issue #171 removes each dead
# entry after the merge of pull request #185.
#
# The two findings below are test fixtures of pull request #160,
# correction round 1. Each value is a fake, fixed marker in a test
# file, never a real credential (see the test file for the value and
# its purpose). The rule "generic-api-key" reads the identifier name
# "val markerPassword = ...", not real entropy of a real secret. A
# later commit renamed the identifier to markerSecondValue.
a8657fbde1d0613c503022741f5542e6f5eb94de:monitor/backend/src/test/kotlin/octometer/monitor/mongo/MongoAppReaderContainerTest.kt:generic-api-key:197
a8657fbde1d0613c503022741f5542e6f5eb94de:monitor/backend/src/test/kotlin/octometer/monitor/mongo/MongoAppReaderUnitTest.kt:generic-api-key:274

# The finding below is a test fixture of pull request #185, the
# security MAJOR 2 correction. The value is a fake, fixed marker user
# name and password for a short auth probe of EndToEndContainerTest,
# never a real credential.
a035b1e9d410e6a123aa897931ea9dfaf20608f5:monitor/backend/src/test/kotlin/octometer/monitor/EndToEndContainerTest.kt:generic-api-key:280
