# MINOR findings of pull request #200

Each row of this file comes from the release review of pull request #200, for issue #68.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #68 | build and release engineer | `.gitattributes:9` | The pattern `/gradlew text eol=lf` is anchored at the repository root. It covers neither `tools/consumer-smoke/spring41/gradlew` nor `tools/consumer-smoke/spring34-maven/mvnw`. | Add the lines `tools/consumer-smoke/spring41/gradlew text eol=lf` and `tools/consumer-smoke/spring34-maven/mvnw text eol=lf`. | open |
| 2026-09-26 | #68 | build and release engineer | The pull request text, acceptance criterion 1, and the same text in comment 5847867144 | Each extract drops the line `org.mongodb:mongodb-driver-core`. The indentation of the rest then reads wrong. | Quote the four lines of the driver block without a gap. | open |
| 2026-09-26 | #68 | build and release engineer | `.github/workflows/ci.yml:177` | The job `consumer-smoke` keeps no test report on a failure, unlike the jobs `jvm` and `monitor-windows`. | Add a step with `if: failure()` that uploads `tools/consumer-smoke/spring34-maven/target/surefire-reports` and `tools/consumer-smoke/spring41/build/reports/tests/test`. | open |
| 2026-09-26 | #68 | build and release engineer | `tools/consumer-smoke/` | The folder holds no README, unlike `tools/demo-app`. A developer cannot learn the order of the commands from the tree. | Add `tools/consumer-smoke/README.md` with `./gradlew publishToMavenLocal` at the root first, then the test command of each project. | open |
| 2026-09-26 | #68 | build and release engineer | `tools/consumer-smoke/spring34-maven/src/main/java/com/octometer/smoke/KitConfiguration.java:15` and the same line of the `spring41` copy | The text "during context start" has no article, and it uses the word "start" as a noun beside the verb "starts" of the same file. | Write "while the context starts". | open |
| 2026-09-26 | #68 | build and release engineer | `tools/consumer-smoke/spring41/build.gradle.kts:5` | The comment sentence about the version 4.1.1 holds about 40 words, over the 25-word limit. | Split it into three sentences. | open |
| 2026-09-26 | #68 | build and release engineer | `tools/consumer-smoke/spring34-maven/src/test/resources/application.properties:2` and `tools/consumer-smoke/spring41/src/test/resources/application.properties:2` | The sentence that starts with "This URI names a port" holds 29 words, over the 25-word limit. | End the sentence after "listener", then start a new sentence. | open |
