# MINOR findings of pull request #215

Each row of this file comes from a review of pull request #215, for issue #69.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #69 | Java and Spring engineer | `kit/jvm-spring/src/test/java/octometer/kit/spring/IngestControllerTest.java:48` | No test proves that the controller reads no body once `beforeBody` already gives a result. | Add a test with a request whose input stream throws on a read, and expect 415 for a `text/plain` request. | open |
| 2026-09-26 | #69 | Java and Spring engineer | `kit/jvm-spring/README.md:87` | The line tells an app to add the JitPack repository, but it omits the `exclusiveContent` limit that design decision D25 needs. | Add an `exclusiveContent` block for the group `com.github.xamcross.octometer` to the snippet. | open |
| 2026-09-26 | #69 | Java and Spring engineer | The pull request text, the section "Test counts (before -> after)" | The text claims 31 moved order tests; the list names 28 methods, and two removed tests are absent from it. The charset entry of the list is a copy of a test, not a move of it. | Change the number 31 to 29, and remove the charset entry from the list. | open |
| 2026-09-26 | #69 | application security engineer | `kit/jvm-spring/README.md:63-77` | The exemption line names no method, and the section holds no warning against a wide `csrf().disable()`. | Name the method in the matcher (for example `new AntPathRequestMatcher("/api/octometer/v1/clicks", "POST")`), then add one sentence against a wide `csrf().disable()`. | open |
| 2026-09-26 | #69 | application security engineer | `kit/jvm-spring/README.md:59` | The README gives the app no CORS rule; a CORS policy that allows a foreign origin would remove the cross-site gate of the 415 check. | Add one sentence that the app must add no CORS allowance of a foreign origin on the ingest path. | open |
| 2026-09-26 | #69 | application security engineer | `tools/consumer-smoke/spring34-maven/src/main/java/com/octometer/smoke/SecurityConfiguration.java:28` | The smoke exemption covers each method of the ingest path; a PUT to that path with no token gives 405, not 403. | Name POST in the matcher too. | open |
| 2026-09-26 | #69 | application security engineer | `tools/consumer-smoke/spring34-maven/src/test/java/com/octometer/smoke/IngestControllerCsrfTest.java:40` | The exempt request and the other request have a different form: the first sends `application/json` and a valid body, the second sends neither. | Add `.contentType(MediaType.APPLICATION_JSON).content(VALID_BODY)` to the post of `/api/smoke/other`. | open |
| 2026-09-26 | #69 | application security engineer | `kit/jvm-spring/src/test/java/octometer/kit/spring/IngestControllerTest.java:231-264` | The two 500 tests check no empty body, though the code answers with no body. | Add `.andExpect(content().string(""))` to `aStoreDefectGives500AndNever400` and to `aResolveUserIdThatThrowsGives500AndNever400`. | open |
| 2026-09-26 | #69 | application security engineer | `kit/jvm-core/src/test/java/octometer/kit/core/ingest/IngestRequestProcessorTest.java`, `kit/jvm-ktor/src/main/kotlin/octometer/kit/ktor/IngestRoute.kt`, `kit/jvm-core/src/main/java/octometer/kit/core/ingest/IngestRequestProcessor.java`, `tools/consumer-smoke/spring41/build.gradle.kts` | Four comments break ASD-STE100 rule 1 (a sentence above 20 to 25 words) and rule 7 (an `-ing` form as a noun). | Split each long sentence, and change "by listing each jar's classes" to "by a list of the classes of each jar". | open |
