# MINOR findings of pull request #216

Each row of this file comes from the review of pull request #216, for issue #114.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #114 | Angular and accessibility engineer | `monitor/frontend/src/app/apps/apps.css:65` | The rule `tbody td { cursor: pointer; }` also covers the new "First pages" cell. That cell holds a link and no click handler, so the pointer cursor promises an action that a click on the cell padding does not give. | Give the new `<td>` a class, and set `cursor: auto` on that class in `apps.css`. | open |
| 2026-09-26 | #114 | Angular and accessibility engineer | `monitor/frontend/src/app/first-pages/first-pages.css:87` | The comment names "the 3:1 floor of WCAG 1.4.3". WCAG 1.4.3 asks 4.5:1 for normal text; 3:1 holds for large text only. | Write "above the 4.5:1 floor of WCAG 1.4.3 for normal text" in the comment. | open |
| 2026-09-26 | #114 | Angular and accessibility engineer | `monitor/frontend/src/app/first-pages/first-pages.html:47` | `aria-current="true"` sits on the static text "Page X of Y". That text names no current item of a set of links or controls, so the attribute adds a wrong announcement. | Remove `aria-current="true"` from that `<span>`. | open |
| 2026-09-26 | #114 | application security engineer | `monitor/frontend/src/app/first-pages/first-pages.html:25` | A path of 10 000 characters gives a link `href` of 10 044 characters. `Application.kt:75` sets no `maxInitialLineLength`, so a reload of that link meets the Ktor Netty default line limit of 4096 bytes, and the server rejects the request. | Issue #115 caps the length of the `firstPath` value that it accepts, and names the cap in its own text. | open |
