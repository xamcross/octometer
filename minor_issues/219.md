# MINOR findings of pull request #219

Each row of this file comes from the review of pull request #219, for issue #30.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer; application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:270-274`; `docs/superpowers/specs/2026-09-21-octometer-design.md:272` | The KDoc and the design note say the backoff caps the next check at 300 seconds, but `PollBackoff.kt` adds a jitter of 10 percent after the cap, so the true maximum is 330 seconds. | Write "a maximum of 330 seconds: the cap of 300 seconds plus the jitter of 10 percent" in both places. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer; application security engineer | `monitor/backend/src/test/kotlin/octometer/monitor/mongo/PrivilegeCheckContainerTest.kt:36-38` | The class KDoc states that the allow-list of `.gitleaks.toml` covers the `MONGO_INITDB_ROOT_PASSWORD` line, but that file holds no such entry; the value is a plain constant reference, so the scan needs no entry. | Delete the two sentences about the allow-list, and write the environment-variable name in plain backticks. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer; application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/PrivilegeCheck.kt:6`; `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:708` | `privilegeVerdict` reuses `REASON_CHECK1_FAILED` ("a collection outside the configured one is visible") for a truncated `listCollections` batch and for an absent cursor field, where no such collection is known, so `last_error` shows a wrong cause. | Add a fourth reason constant for the no-evidence case of a truncated or an absent cursor (for example "privilege check 1 failed: the visible collection list is not complete"), and return it from the two cursor branches. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer; application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:136` | `PollTarget.checkPrivileges` defaults to `false`. Only `PollScheduler.startPoll` builds a `PollTarget` in production today, and it always passes the computed value, but a future call site of `pollOnce` that omits the argument would skip the privilege check in silence. | Drop the default value, and pass the explicit flag at each call site, including each test. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer; application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/poll/PollScheduler.kt:256`; `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:268-274`; `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:691` | Several KDoc sentences of the privilege-check code pass the 25-word bound of ASD-STE100 rule 1, and one sentence joins two statements with "and" (rule 4). | Split each long sentence into sentences of a maximum of 25 words, with one statement in each. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:700` | `privilegeVerdict` has no direct unit test, so its `cursor == null` branch and its `cursor.id != 0` branch run in no test today, only indirectly through the container test. | Add two `PrivilegeCheckTest` cases that call `privilegeVerdict` with a cursor id of `1`, and with no `cursor` field. | open |
| 2026-09-27 | #30 | Kotlin and MongoDB engineer | `monitor/backend/src/main/kotlin/octometer/monitor/poll/PollScheduler.kt:340` | The choice of `readFailure.reason` for `last_error` has no test at the scheduler level; the container test writes the app row itself, so it proves nothing of the scheduler path. | Add a `PollSchedulerTest` case with a stub cycle that throws `MongoReadFailedException(STATUS_OVERPRIVILEGED, code = null, reason = REASON_CHECK2_FAILED)`, then assert `store.lastErrorOf(appId)`. | open |
| 2026-09-27 | #30 | application security engineer | `monitor/backend/src/main/resources/logback.xml:56-62` | The comment gives the connection string as the only reason for the WARN pin of `org.mongodb`, but the pin also keeps the `connectionStatus` answer out of the log; a probe at DEBUG showed that answer in two driver lines. | Add one sentence that names design decision D9 and the privilege answer, so a later change of that level sees the second reason. | open |
| 2026-09-27 | #30 | application security engineer | `monitor/backend/src/test/kotlin/octometer/monitor/mongo/PrivilegeCheckContainerTest.kt:120-121` | The test writes the `OVERPRIVILEGED` status itself with `eventStore.recordFailure`, then it asserts that same status; the assertion cannot fail for a reason of the privilege check itself. | Delete the two lines, or add a comment that names the `EventStore` write as the object under test. | open |
