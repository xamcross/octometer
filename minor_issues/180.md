# MINOR findings of pull request #180

Each row of this file comes from the review of pull request #180, for issue #177.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-22 | #177 | application security engineer | `monitor/backend/build.gradle.kts:109` (also `kit/jvm-mongo/build.gradle.kts:80`, `tools/demo-app/build.gradle.kts:71`) | The Docker probe never drains the output pipe of the child process, so a long output can hang the build. | Redirect the output and the error stream to DISCARD, not to one merged stream. | open |
| 2026-09-22 | #177 | application security engineer | `monitor/backend/build.gradle.kts:109` (also `kit/jvm-mongo/build.gradle.kts:80`, `tools/demo-app/build.gradle.kts:71`) | The Docker probe runs once for each test task, instead of once for the whole build. | Compute the value once in a `ValueSource`, or once in the root build script. | open |
