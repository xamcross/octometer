# MINOR findings of pull request #211

Each row of this file comes from the review of pull request #211, for issue #31.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #31 | Kotlin and Ktor engineer | `monitor/backend/src/main/kotlin/octometer/monitor/Application.kt:128-133` | The `try`/`catch` path decode is a second copy of `RequestGuard.kt:125-129`. The two can drift, and a drift can give a log leak. | Add `internal fun decodedPathOrNull(rawPath: String): String?` to `RequestGuard.kt`. Let `isApiPath` and the `CallLogging` format function both call it. | open |
| 2026-09-26 | #31 | Kotlin and Ktor engineer | `monitor/backend/src/main/kotlin/octometer/monitor/Application.kt:44-48,110-115,121-124` | Three comment sentences break ASD-STE100: one holds 54 words and joins two statements with a semicolon, one holds 30 words and joins two instructions with "and", one holds 29 words. | Split each of the three sentences into shorter ones, each with one instruction. | open |
| 2026-09-26 | #31 | Kotlin and Ktor engineer | `monitor/backend/src/test/kotlin/octometer/monitor/ConnectionStringSentinelContainerTest.kt:376` | The field `SentinelResponses.appId` takes a value, and no assertion reads it. | Remove the field and its argument. | open |
| 2026-09-26 | #31 | Kotlin and Ktor engineer | `monitor/backend/src/test/kotlin/octometer/monitor/ConnectionStringSentinelContainerTest.kt:142` | `goodMongoClient` opens, and `monitorServer.start` runs, both before the `try` block. A throw of `start`, for example a port clash, leaks the client and its threads. | Move the `start` call into the `try` block. | open |
| 2026-09-26 | #31 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/Application.kt:127` | RFC 9112 allows an absolute-form request target. `call.request.path()` then returns the authority too, so the log line can hold attacker text in place of a path. | Log `INVALID_PATH_LOG_TEXT` when `rawPath` does not start with `/`. | open |
| 2026-09-26 | #31 | application security engineer | `monitor/backend/src/test/kotlin/octometer/monitor/ConnectionStringSentinelContainerTest.kt:278,304` | The sentinel search has two small gaps: `createApp` reads the 201 body but does not check it for a marker, and `assertNoMarkerInLogs` walks `throwableProxy.cause` but not `throwableProxy.suppressed`. | Assert in `createApp` that the 201 body holds neither marker. Walk `throwableProxy.suppressed` beside `cause`. | open |
| 2026-09-26 | #31 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/Application.kt:110-115`, `monitor/backend/src/test/kotlin/octometer/monitor/ConnectionStringSentinelContainerTest.kt:36-41,61-65`, `gradle/libs.versions.toml:41-42`, `monitor/backend/src/test/kotlin/octometer/monitor/CallLoggingTraceTest.kt:14-16` | Five new comments break ASD-STE100: a semicolon join, a passive form, a long sentence with no list, a sentence with no verb and no article, and an `-ing` form used as a verb. | Split each long sentence. Write the active voice. Use a vertical list for the four search targets. Add the missing verb and article. Replace each `-ing` form with a plain verb. | open |
| 2026-09-26 | #31 | Kotlin and Ktor engineer | `monitor/backend/src/test/kotlin/octometer/monitor/CallLoggingTraceTest.kt:132-141` | The `CallLogging` plugin writes its line from the Netty event loop. A read of the capture list right after the raw socket read can race that write. The 403 case failed once in five runs with "the capture read no log event". | Poll the capture list for the expected line, with a bound of two seconds, before the capture block ends. | fixed in #211 |
