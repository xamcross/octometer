# MINOR findings of pull request #207

Each row of this file comes from the review of pull request #207, for issue #29.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #29 | Kotlin coroutines engineer | `PollSchedulerTest.kt:790` | `awaitCondition` calls only `runCurrent()`, so no tick runs after the wait; a broken reset of `plannedTickAt` still keeps the test green. | Add `repeat(3) { testScheduler.advanceTimeBy(1_000); testScheduler.runCurrent() }` after that line. | open |
| 2026-09-26 | #29 | Kotlin coroutines engineer | `PollSchedulerTest.kt:800` | `contains("15")` also matches a lateness in milliseconds (`15000`), so it does not prove the unit. | Assert the whole line with `assertEquals("resume detected. 15", resumeLines.single().formattedMessage)`. | open |
| 2026-09-26 | #29 | Kotlin coroutines engineer | `PollScheduler.kt:241` | A second sleep inside the 15-second wait ends the `delay` at the wake; the next poll then starts with no fresh wait. | Make the check a `while` loop, so a gap that grows during the wait starts one more wait. | open |
