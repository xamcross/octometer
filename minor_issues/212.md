# MINOR findings of pull request #212

Each row of this file comes from the two reviews of pull request #212, for issue #113.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #113 | Kotlin, Ktor, and SQLite engineer | `SessionsRouteTest.kt:199` | No test fails when the offset moves from a bound parameter into the SQL text. | Assert that `SESSION_PAGE_SQL` and `SESSION_PAGE_FILTERED_SQL` each end with the text `LIMIT ? OFFSET ?`. | open |
| 2026-09-26 | #113 | Kotlin, Ktor, and SQLite engineer | `SessionsRoute.kt:30, 85, 182` | Three sentences hold 38, 31, and 35 words; rule 1 of the language policy allows 25 words in a description. | Split each of the three sentences into two sentences of a maximum of 25 words. | open |
| 2026-09-26 | #113 | application security engineer | `SessionsRoute.kt:38` | The KDoc of `SessionRow` does not say that `firstPath` and `source` hold untrusted text of the customer app. | Add one KDoc sentence that `firstPath` and `source` hold untrusted text of the customer app, and that the frontend of #115 must render each one as text. | open |
| 2026-09-26 | #113 | application security engineer | `SessionsRouteTest.kt:473` | The sentinel sent one 200 request only, and an error answer writes the most log lines. | Send a 400, a 403, and a 404 request inside the same `captureLogEvents` block. | open |
| 2026-09-27 | #113 | Kotlin, Ktor, and SQLite engineer (round 2) | `SessionsRoute.kt:84` | The comment states "This statement runs on the index event_agg", but the shipped statement runs on `event_session (app_id=?)` with no statistics; `event_agg` appears only for the bare text after an `ANALYZE`. | State that `event_agg` is the intended index, and name the plan that the shipped configuration gives. | open |
