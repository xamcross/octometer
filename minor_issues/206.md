# MINOR findings of pull request #206

Each row of this file comes from the review of pull request #206, for issue #44.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #44 | application security engineer | `.github/workflows/release.yml:110` | The second `setup-node` step sits before the step `Build the release zip`, so the comment at line 103 no longer states the truth, though the build result does not change. | Move the three steps of lines 110 to 138 to a position after the step `Find the built zip`. | open |
| 2026-09-26 | #44 | application security engineer | `.github/workflows/release.yml:83` | The `grep` and `sed` read of the version depends on the key order of `kit/tracker/package.json`. | Read the value with `node -p "require('./kit/tracker/package.json').version"`. | open |
| 2026-09-26 | #44 | application security engineer | `.github/workflows/release.yml:117` | The step at line 120 runs third-party build code inside the job that holds `contents: write`, before the zip build and the attach step. | Build both assets in a job with `permissions: contents: read`, then pass them through `actions/upload-artifact`. | open |
| 2026-09-26 | #44 | application security engineer | `.github/workflows/release.yml:119` | The comment uses "attach" as a verb and as a noun, against ASD-STE100 rule 6. | Write "because the step that attaches the asset runs on a push only." | open |
| 2026-09-26 | #44 | application security engineer | `kit/tracker/README.md:8` | "The tarball sits on the GitHub Release" is a metaphor, and "No npm account and no token are needed" is the passive voice. | Write "The GitHub Release of the matching tag holds the tarball. You need no npm account and no token." | open |
| 2026-09-26 | #44 | application security engineer | `kit/tracker/README.md:15` | A semicolon joins two statements in one sentence. | Write "The release `0.1.0` holds no tarball. A later tag brings the first one." | open |
