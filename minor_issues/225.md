# MINOR findings of pull request #225

Each row of this file comes from the review of pull request #225, for issue #115.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-27 | #115 | Angular and accessibility engineer | `monitor/frontend/src/app/sessions/sessions.html:61` | The current-page text of the pager holds no `aria-current` value. | Change line 61 to `<span aria-current="true">Page {{ currentPage() }} of {{ page.pageCount }}</span>`. | open |
| 2026-09-27 | #115 | Angular and accessibility engineer | `monitor/frontend/src/app/sessions/sessions.spec.ts:445` | The middle-page test asserts the three pager texts, and no `aria-disabled` state. | Add one assertion for Previous and one for Next: each button drops the attribute in the middle. | open |
| 2026-09-27 | #115 | Angular and accessibility engineer | `CHANGELOG.md:18` | The text "with a link from the user list and from the element view" names a link that does not exist; the element view holds no link to the new view. | Write "with a link from the user list, and a link to the element view." | open |
| 2026-09-27 | #115 | Angular and accessibility engineer | commit `e7a4446`, the commit body | The sentence "so a visitor can confirm the session before it reads the rows" uses "it" for a person, and "visitor" also names the tracked person, not the operator. | Use "so the operator can confirm the session before the operator reads the rows" in each later text. | open |
| 2026-09-27 | #115 | application security engineer | `monitor/frontend/src/app/sessions/sessions.spec.ts:206` | The suite proves the escape of `firstPath` and of `source`, but not of `sessionId` and not of `userId`. | Add one test with a hostile `sessionId` and a hostile `userId`; assert the two cell texts, and assert the anchor `href` starts with `/apps/`. | open |
| 2026-09-27 | #115 | application security engineer | `monitor/frontend/src/app/sessions/sessions.ts:105` | The comment sentence beginning "Old rows stay on the screen" holds 35 words, joins two statements with "and", and uses the idiom "in flight". | Write three short sentences, and replace "in flight" with "while the request waits for an answer". | open |
