# MINOR findings of pull request #222

Each row of this file comes from the review of pull request #222, for issue #70.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:35-38` | The sentence of the Maven proof has 30 words. | Split the sentence after "pom.xml`" and list the three parts of the proof as a vertical list. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:259-261` | The sentence about the CORS header and the same origin has 32 words. | Write two sentences, one for the missing CORS header and one for the same origin of the note. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:143-146` | The driver version of the reader stays open; Spring Boot 3.4.1 manages `mongodb-driver-sync` 5.2.1. | Add one sentence that names the driver version of the reader's Spring Boot version, and point a Spring Boot 3.4 reader at `timeoutMS`. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:279` | The `-A` option of the check command has no stated reason. | Add the sentence that the kit drops a batch of a known robot user agent, or link section 8 of the Ktor guide for it. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/superpowers/specs/2026-09-21-octometer-design.md:422` | The new D25 sentence names the mirror alone, not the manager behind it. | Write "the `settings.xml` mirror of a repository manager" in the D25 sentence. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:190-192` | The proxy prefix stays abstract; the reader does not learn that the default path already sits below `/api`. | Add that the default path already sits below an `/api` prefix, so a proxy that forwards `/api` needs no new rule. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:18-20` | The sentence "Use tag `0.2.0` for each kit module of your app (design decision D26): one tag builds each module together, so a mixed pair of tags never happens." has 27 words. | Write the rule and the reason as two sentences. | open |
| 2026-09-27 | #70 | Java and Spring Security engineer | `docs/integration-spring.md:189-192` | The sentence "A JWT resource server may read the `sub` claim in its place, only when that claim already holds an opaque internal id, and never an email address." has 27 words. | Write the condition as a second sentence. | open |
