# MINOR findings of pull request #173

Each row of this file comes from the review of pull request #173, for issue #27.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-22 | #27 | Kotlin, MongoDB, and SQLite engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:440` | `invalidReason` never checks the BSON type of the `_id` field, so a wrong type throws an exception. | Add a `$type` term of `objectId` to the `_id` filter, and note the reason. | open |
| 2026-09-22 | #27 | Kotlin, MongoDB, and SQLite engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:469` | `invalidReason` checks the presence and the type of each field only, not the length or the pattern rules. | Reject an empty `element` string, and file a follow-up issue for the other rules. | open |
| 2026-09-22 | #27 | Kotlin, MongoDB, and SQLite engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:210` | No test proves that a failed cycle leaves the three status columns unchanged, or that an empty cycle writes OK. | Add both tests to `MongoAppReaderContainerTest`. | open |
| 2026-09-22 | #27 | Kotlin, MongoDB, and SQLite engineer | `monitor/backend/src/main/kotlin/octometer/monitor/store/EventStore.kt:31` | A good poll cycle never resets the `app.consecutive_failures` column to zero. | Add `consecutive_failures = 0` to `RECORD_CYCLE_SUCCESS_SQL`, or name the reset in issue #28. | open |
| 2026-09-22 | #27 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:455` | The KDoc of `invalidReason` claims more checks than the code runs, past the scope of design decision D5. | State that the function checks the presence and the BSON type only. | open |
| 2026-09-22 | #27 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/store/EventStore.kt:34` | One KDoc paragraph holds seven sentences, over the six-sentence limit of the language policy. | Split the paragraph with a blank comment line before the transaction sentence. | open |
| 2026-09-22 | #27 | application security engineer | `monitor/backend/src/test/kotlin/octometer/monitor/mongo/MongoAppReaderContainerTest.kt:39` | A split sentence still joins two statements with a semicolon. | Write two sentences: each test needs Docker, and a machine with no Docker skips the class. | open |
| 2026-09-22 | #27 | application security engineer | pull request text and issue comment (no file on `main`) | The pull request text and the issue comment use the passive voice, and one uses an `-ing` form. | Rewrite each sentence in the active voice, for example "a temporary break proved each check." | open |
| 2026-09-22 | #27 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/retention/RetentionPurge.kt:29` | The retention purge deletes from the `event` table only, so the `skipped_event` table grows with no bound. | Add the `skipped_event` table to the retention purge, with the same retention as `event`. | open |
| 2026-09-22 | #27 | application security engineer | `monitor/backend/src/main/kotlin/octometer/monitor/mongo/MongoAppReader.kt:385` | A document whose `_id` is not an `ObjectId` never reaches `skipped_event`, with no comment that states why. | State that the filter of section 4.3 returns an `ObjectId` `_id` only. | open |
