# MINOR findings of pull request #198

Each row of this file comes from the review of pull request #198, for issue #39.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #39 | application security engineer | `.github/workflows/release.yml:26-33` | One job runs the third-party build code and the publish steps under `contents: write`, and a dry run holds the same write permission. | Move the three push-only steps into a second job with `contents: write`, and pass the zip with `actions/upload-artifact`. | open |
| 2026-09-26 | #39 | application security engineer | `.github/workflows/release.yml:113` | The release publishes a 47 MB zip with no checksum and no provenance statement. | Add a `sha256sum` file as a second asset, or add `actions/attest-build-provenance` with `attestations: write`. | open |
| 2026-09-26 | #39 | application security engineer and build and release engineer | `.github/workflows/release.yml:29,129-137` | Three JitPack modules with a bound of 600 seconds each sum to 30 minutes, equal to `timeout-minutes: 30`, so GitHub can cancel the job before its clear message prints. | Raise `timeout-minutes` to 60, or lower the per-module deadline to 300 seconds, and give each `curl` call `--max-time 30`. | open |
| 2026-09-26 | #39 | application security engineer | `.github/workflows/release.yml:126` | One comment uses "builds" as a verb and "build" as a noun, against ASD-STE100 rule 6. | Write "Each module below waits for its JitPack job, with a bound of 10 minutes." | open |
| 2026-09-26 | #39 | application security engineer | `.github/dependabot.yml:111` | The comment names `.github/workflows/ci.yml` only, though the repository now holds a second workflow file. | Write "The GitHub Actions of each file in `.github/workflows/`." | open |
| 2026-09-26 | #39 | build and release engineer | `.github/workflows/release.yml:4` | The comment says the `workflow_dispatch` trigger gives a dry run with no tag, though the trigger needs the required input `tag`. | Write "The workflow_dispatch trigger gives a dry run. The dry run creates no release." | open |
| 2026-09-26 | #39 | build and release engineer | `.github/workflows/release.yml:95` | The comment says "The two steps below", but three steps below hold `if: github.event_name == 'push'`. | Write "The three steps below". | open |
| 2026-09-26 | #39 | build and release engineer | `CHANGELOG.md:8,18`, `.github/workflows/release.yml:56,102` | The Keep a Changelog example writes each heading with brackets, but this file omits them. | Add the brackets to each heading, and update the `grep` pattern and the `awk` pattern to match them. | open |
| 2026-09-26 | #39 | build and release engineer | `CHANGELOG.md:64` | The word "flaky" is informal, outside ASD-STE100. | Write "Make the test pause-refresh-integration.spec.ts stable. (#100)". | open |
| 2026-09-26 | #39 | build and release engineer | `docs/demo.md:84` | The document names the old output file `backend-0.1.0.zip`, which this pull request renames. | Write `octometer-monitor-0.1.0.zip`. | open |
