# MINOR findings of pull request #208

Each row of this file comes from a review of pull request #208, for issue #187.

| Date | Issue | Reviewer role | File and line | Finding | Suggested fix | Status |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `AppRegistryService.kt:269` | The comment says "neither string reaches this line"; the two strings are the parameters on line 270. | Write "no log line and no exception message holds either string." | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `docs/superpowers/specs/2026-09-21-octometer-design.md:261-264` | The first new sentence holds 38 words; ASD-STE100 gives a description a maximum of 25 words. | Name the seven columns in a vertical list, or split the sentence in two. | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `docs/superpowers/specs/2026-09-21-octometer-design.md:263-264` | The text says "the new source then reads each event", and the reader reads, not the source. | Write "the reader then reads each event of the new source, from the oldest one". | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `AppRegistryServiceTest.kt:375` | The test proves the propagation of the failure only, not that a failed write of the new string changes no column. | Add `seedPollState` and one `assertEquals(before, readPollState(appId))` to that test. | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `monitor/backend/src/main/resources/db/001_init.sql:10` | The `app` row also holds `privileges_checked_at`, and the reset keeps that column; no production code writes it today (B03, #30). | Add one sentence beside D10 that issue #30 adds `privileges_checked_at` to the reset list. | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `CHANGELOG.md:16` | The section "Unreleased / Monitor" holds no line for this fix. | Add "Reset the cursor on a PATCH of the connection string to a different source. (#187)". | open |
| 2026-09-26 | #187 | application security engineer | `AppRegistryService.kt:197` | The reset runs after the secret write; a failed reset write leaves the new secret with the old cursor, and a retry with the same string skips the reset (equal hash). | Move the `resetPollState` call before `secretStore.put`; an extra reset costs time only (D10). | open |
| 2026-09-26 | #187 | application security engineer | `AppRegistryService.kt:143, 145, 273` and the body of commit `e8d80c9` | Four sentences hold 27, 29, 31, and 37 words; two join two statements with "and" (ASD-STE100 rules 1 and 4). | Split each of the four sentences into two short sentences. | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `docs/superpowers/specs/2026-09-21-octometer-design.md:277-280` | The new sentence holds about 55 words, and "in flight" is a metaphor (ASD-STE100 rules 1 and 11). | Split it into three short sentences, and write "a cycle that already runs". | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `EventStore.kt:60-71` and `MongoAppReader.kt:400-408` | Three new KDoc sentences hold 31 to 34 words; ASD-STE100 gives a description a maximum of 25 words. | Split each one into two sentences. | open |
| 2026-09-26 | #187 | Kotlin and SQLite engineer | `EventStore.kt:198-204` | The reject path calls `rollback(writer, null)`, so `failure?.addSuppressed(rollbackError)` drops a failed `ROLLBACK` in silence; the comment above states the opposite rule. | Log one WARN in `rollback` when `failure` is `null`. | open |
