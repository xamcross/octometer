# The Gradle build cache (issue #177, step 1).
# A test task of an unchanged module reuses its cached result. The cache
# lives in %USERPROFILE%\.gradle\caches\build-cache-1, so a fresh clone of
# the owner reuses the result of an earlier clone.
org.gradle.caching=true
