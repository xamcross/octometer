import { Component } from '@angular/core';

/**
 * Placeholder view for the /manage route.
 * A later issue adds the app list and the app form (D32).
 */
@Component({
  selector: 'app-manage',
  templateUrl: './manage.html',
})
export class Manage {}
