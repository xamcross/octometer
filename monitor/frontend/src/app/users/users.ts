import { Component } from '@angular/core';

/**
 * Placeholder view for the /apps/:appId/users route.
 * A later issue adds the real user table (D28, level 2).
 */
@Component({
  selector: 'app-users',
  templateUrl: './users.html',
})
export class Users {}
