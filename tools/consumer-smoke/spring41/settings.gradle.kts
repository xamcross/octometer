// The consumer smoke project for Spring Boot 4.1 with Gradle (issue #68).
// This build stands on its own. It is not a project of the root build
// (settings.gradle.kts of the repository root has no include for it).
rootProject.name = "spring41"
