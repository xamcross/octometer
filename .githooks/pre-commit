#!/bin/sh
# The secret scan of a commit.
#
# This hook runs before each commit. It scans the staged changes for a
# secret. It uses gitleaks and the rules of .gitleaks.toml.

gitleaks protect --staged --redact -v
